# Matter budget is the chemistry rung

**Pin:** live SHA prefix of `vendor/fsot_compute.py`  
**Not:** restoring Quantum_Mechanics to assigned \(D=6\). Not a decimal polish on \(\lvert S_{\mathrm{cosm}}\rvert(1-S_{\mathrm{quant}})\).

## What collapsed

The nest law \(D_{\mathrm{eff}}(g)=\mathrm{round}(5\cdot 5^{g/(G-1)})\) puts Particle at \(g=0\) and Quantum_Mechanics at \(g=1\). Both round to **\(D=5\)**. Same look \(1\), same hits \(0\), same specimen, same unused \(C\). So

\[
S_{\mathrm{quant}}=S_{\mathrm{particle}}.
\]

The old baryon formula \(\lvert S_{\mathrm{cosm}}\rvert(1-S_{\mathrm{quant}})\) was written when QM was *assigned* \(D=6\) with default look. After the nest, that formula is “cosmology times the complement of the particle floor.” That is the wrong object for \(\Omega_b h^2\).

Planck \(\Omega_b h^2\) is the cosmological **inventory of atoms** (baryons that make chemistry), not free-orbit quantum mechanics.

## Unique nest identification

The first default-look specimen *above* the particle floor:

| Orifice | \(D\) | look | hits | observed | Role |
|---------|------:|------|-----:|----------|------|
| Particle_Physics | 5 | \(1\) | 0 | specimen | floor |
| Quantum_Mechanics | 5 | \(1\) | 0 | specimen | same floor (nest collapse) |
| Atomic_Physics | 6 | \(e/\pi\) | 0 | specimen | **bound-well look-split** |
| High_Energy_Physics | 6 | \(1-\mathrm{POOF}/\pi\) | 1 | specimen | **collision look-split** |
| **Physical_Chemistry / Chemistry** | **6** | **\(1\)** | **0** | **specimen** | **default-look atoms** |

Atomic and HEP are look-splits of generation \(g=2\). The default-look specimen of generation \(g=3\) (same \(D=6\)) is Chemistry. \(C\) does not enter \(S\), so Physical_Chemistry and Chemistry are the same scalar.

That uniquely identifies the baryon object. It is not a residual search across Nuclear, Biology, or Cosmology.

## Live formulas

\[
\Omega_b h^2=\lvert S_{\mathrm{cosm}}\rvert\,(1-S_{\mathrm{chem}})
\]

\[
\Omega_c h^2=(1-S_{\mathrm{chem}})\,\varphi\,A_{\mathrm{in}}
\]

Dark-matter density is the sibling complement at the **same** rung: what did not inventory as atoms. Electroweak and geometry objects stay on \(S_{\mathrm{quant}}\) (particle floor) or \(S_{\mathrm{cosm}}\).

## What we did not do

- Did not re-assign QM to \(D=6\) to hide the 11.84% miss.
- Did not shop Nuclear / Atomic / HEP because a coefficient looked greener.
- Did not rewrite dated-forecast JSON.
- Did not put \(0.99\) back into \(K\).

The superseded QM-class baryon object (\(\lvert S_{\mathrm{cosm}}\rvert(1-S_{\mathrm{quant}})\), **11.84%**) stays in [`../results/MISSES.md`](../results/MISSES.md).

Hubble \(H_0\) (1.55%) is a different object: [`H0_DUAL_OBJECT.md`](H0_DUAL_OBJECT.md). \(\alpha_s(M_Z)\): wave-1 geometric \(1/(e\pi)\) is the Ledger A freeze (0.679%); the QCD process orifice is \(2(\mathrm{POOF}/\psi_{\mathrm{con}})^2\) (0.0075%). Freeze not rewritten.
