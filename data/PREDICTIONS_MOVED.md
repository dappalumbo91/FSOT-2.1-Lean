# Predictions moved

All prediction manifests, freezes, atlases, and monitor reports now live in:

**[`predictions/`](../predictions/README.md)**

Do not add new prediction artifacts under `data/`. Use `predictions/` (and `predictions/reports/` for markdown).
