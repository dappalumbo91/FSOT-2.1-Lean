# Tier 22 — Magnetosphere coupling (Dst + Kp + magnetic-string crosswalk).
version: "1.0"
updated: "2026-07-03"

source:
  geomagnetism_cache: data/geomagnetism_swpc_cache.json
  kp_cache: data/space_weather_cache/kp_index.json
  magnetic_S_em: 0.5188655207983146
  dst_storm_threshold: -30.0
  kp_storm_threshold: 5.0
  kp_primary_resolution: rolling_6h_max
  kp_scalar_multiplier: 0.35

classifier_mode: union

build:
  benchmark: scripts/build_magnetosphere_benchmark.py
  output: data/magnetosphere_benchmark.json

lean:
  generator: scripts/gen_magnetosphere_lean.py
  module: FSOT.Formal.MagnetospherePriors
  lean_domain: electron
  sign_theorem: electron_raw_S_positive

verification:
  min_records: 40
  min_stability_match_rate: 0.5