# Math generator cross-domain FSOT formula comparisons.
version: "1.1"
updated: "2026-07-09"
math_generator_root: vendor/math_generator

artifacts:
  comparison_report:
    path: generated_formula_comparison_report.json

verification:
  comparison_count_min: 5
  max_error_pct: 5.0