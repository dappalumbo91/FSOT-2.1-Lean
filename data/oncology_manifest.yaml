# Tier 26 — SMILES drug/enzyme affinity + biology strict operon bridge.
version: "1.0"
updated: "2026-07-03"

D_eff: 14

build:
  benchmark: scripts/build_oncology_benchmark.py
  output: data/oncology_benchmark.json

lean:
  generator: scripts/gen_oncology_lean.py
  module: FSOT.Formal.OncologyPriors
  lean_domain: medical
  sign_theorem: medical_raw_S_positive

verification:
  min_records: 20
  max_median_error_pct: 5.0